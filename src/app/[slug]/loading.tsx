export default function RestaurantLoading() {
  return (
    <main className="grid min-h-screen place-items-center bg-[#17231a] text-white">
      <div className="text-center">
        <span className="mx-auto block size-12 animate-pulse rounded-full bg-[#d7ef58]" />
        <p className="mt-5 font-mono text-xs uppercase tracking-[0.2em] text-white/60">
          Preparando la mesa
        </p>
      </div>
    </main>
  );
}
